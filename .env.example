VITE_IS_MAINTENANCE_MODE='false'

# Turso libSQL credentials for the save-list Netlify function.
# Optional: if unset, save-list falls back to an in-memory store (see README).
TURSO_DB_URL=
TURSO_AUTH_STRING=
